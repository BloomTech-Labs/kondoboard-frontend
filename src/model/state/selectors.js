export const selectSavedJobList = state => state.savedJobList;

export const selectSavedJobIds = state => state.savedJobIds;

export const selectJobList = state => state.jobList;

export const selectUserQuery = state => state.userQuery;

export const selectSavedJob = state => state.job;

export const selectUser = state => state.user;

export const selectUserId = state => state.user.id;

export const selectJobTags = state => state.tags;

export const selectTaggedJobs = state => state.taggedJob;

export const selectAppliedJobs = state => state.appliedJobList;

export const selectJobColumns = state => state.columns;