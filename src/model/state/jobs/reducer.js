import * as Actions from './action.js';

const initialState = {
    jobList: [],
    job: {

    }
};

export const reducer = (state = initialState, action) => {
    switch(action.type) {
        default:
            return state
    }
}